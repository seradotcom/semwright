# Transports

Primary target: official Figma Plugin API through authenticated localhost bridge.

Optional future REST is for metadata, versions, comments, team/project/library reads and cloud exports, only with proper secret references. It is not required for ordinary editing. Figma MCP is a federation alternative, not the first-party driver.

Unsupported production transports: CDP, app.asar patching, arbitrary eval and private endpoints.
